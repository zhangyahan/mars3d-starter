使用 `vite` 搭建的地图项目脚手架，包含地图 API 为：

- [cesiumjs](https://cesium.com/platform/cesiumjs/)
- [arcgis-js-api](https://developers.arcgis.com/javascript/latest/)

相比较于 `webpack` 它会在热更新时更快，因为 `vite` 使用的时原生的 `esm` 模块加载方式。
